services: 

  db:
    image: postgres:16.1

    ports: 
      - "5432:5432"

    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres

    restart: always
    volumes: 
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: